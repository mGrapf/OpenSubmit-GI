#include <iostream>
using namespace std;

int main(){
/* Erstellen Sie einen minimalen Taschenrechner. Vom Nutzer werden
 * folgende drei Eingaben verlangt: operand1, operator und operand2.
 * (Beispiel für eine Eingabe: 1,5 * 120).
 * Die Operanden sind Fließkommazahlen, der Operator ein Zeichen. Gehen
 * Sie davon aus, dass der Operator eines der Zeichen +, -, *, / ist.
 * Das Programm soll das Ergebnis der Rechenaufgabe berechnen und
 * ausgeben. Geben Sie im Fehlerfall "Fehler" aus.
 */
	return 0;
}
